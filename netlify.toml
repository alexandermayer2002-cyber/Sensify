[build]
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "REACT_APP_SUPABASE_URL,SUPABASE_URL"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# PAUSED until A2P 10DLC campaign is approved — every send was being blocked
# by carriers (undelivered) and still billed. Re-enable by uncommenting the
# schedule line below once the campaign clears.
# [functions."daily-sms-scheduler"]
#   schedule = "0 * * * *"
